function y=GD(f,d,para)
    str=[u1,u2,u3,u4,u5,u6];
    z=fun(f,d)
    y=[]
    for i=1:d
             subs(z(i),str(1:d))
%          f1=matlabFunction(z(i));
         y(i)=f1(para(i));
    end
   
end

function z=fun(f,d,para)
    syms u1 u2 u3 u4 u5 u6                               
    str=[u1,u2,u3,u4,u5,u6];
    y = f(str(1:d));
    syms z1 
    z=[z1]
    for i=1:d
        z(i)=diff(y,str(i))
        
    end
end